-------------
dependencies:
-------------
for all projects:
    - MSVC: https://visualstudio.microsoft.com/

for x86_real/protected projects:
    - nasm: https://nasm.us/
    - qemu: https://www.qemu.org/
    - bochs: https://sourceforge.net/projects/bochs/
    - OpenWatcom: https://sourceforge.net/projects/openwatcom/ [to be removed]

for building our gcc cross compiler:
    - cygwin
    - gcc-13.2.0 source code
    - binutils-2.41 source code

-------------------------------------------
how to install nasm, watcom, bochs and qemu
-------------------------------------------
- downlaod all of them
- install them in the folders:
    - tools/nasm
    - tools/watcom
    - tools/bochs
    - tools/qemu

-----------------------------------------------
how to setup a windows-based gcc cross compiler
-----------------------------------------------
- download and install cygwin and some packages:
    - wget, tar, make, nasm, gcc-g++, bison, flex, libgmp-devel, libmpc-devel, libmpfr-devel, texinfo
- download latest binutils and gcc source archives, then extract them to output/
- open cygwin command shell C:\cygwin64\Cygwin.bat and switch to output/ folder
- setup some environment variables by performing those commands:
    - export PREFIX="$PWD/../tools/i686-elf"
    - export TARGET=i686-elf
    - export PATH="$PREFIX/bin:$PATH"
- create the binutils-build and gcc-build directories, then switch to binutils-build
- configure binutils with the following flags:
    - ../binutils-2.41/configure --target=$TARGET --prefix=$PREFIX --with-sysroot --disable-nls --disable-werror
- build binutils:
    - make -sj12
    - if building binutils fails with the latest version,
      switch to an older version or 2.41 (guaranteed to work under cygwin)
- install binutils:
    - make install
- switch to gcc-build folder then configure gcc:
    - ../gcc-13.2.0/configure --target=$TARGET --prefix=$PREFIX --disable-nls --enable-languages=c,c++ --without-headers
- build gcc:
    - make all-gcc -sj12 [currently here]
    - make all-target-libgcc -sj12
    - if building gcc fails with the latest version,
      switch to an older version or 13.2.0 (guaranteed to work under cygwin)
- install gcc:
    - make install-gcc
    - make install-target-libgcc
- binutils and gcc cross compiler are now installed under tools/i686-elf

# when compiling with GCC always use these flags:
    -std=c99 -g